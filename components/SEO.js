import Head from 'next/head';

export default function SEO() {
  return (
    <Head>
      <meta name="viewport" content="width=device-width, initial-scale=1" />
      <meta name="keywords" content="resume, ATS, checker, analysis" />
      <link rel="icon" href="/favicon.ico" />
    </Head>
  );
}
